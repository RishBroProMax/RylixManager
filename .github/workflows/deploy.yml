name: Build Additional Images

on:
  push:
    branches: [main, canary]
  workflow_dispatch:

permissions:
  contents: read
  packages: write

jobs:
  build-and-push-schedule-image:
    runs-on: ubuntu-latest
    env:
      DOCKERHUB_USER: ${{ secrets.DOCKERHUB_USERNAME || 'imrishmika' }}
      DOCKERHUB_TOKEN: ${{ secrets.DOCKERHUB_TOKEN }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Log in to Docker Hub
        if: env.DOCKERHUB_TOKEN != ''
        uses: docker/login-action@v3
        with:
          username: ${{ env.DOCKERHUB_USER }}
          password: ${{ env.DOCKERHUB_TOKEN }}

      - name: Set tag and version
        id: meta-schedule
        run: |
          VERSION=$(jq -r .version apps/dokploy/package.json)
          REPO_LOWER=$(echo "${{ github.repository }}" | tr '[:upper:]' '[:lower:]')
          TAG="latest"
          if [ "${{ github.ref }}" != "refs/heads/main" ]; then
            TAG="canary"
          fi
          TAGS="ghcr.io/${REPO_LOWER}-schedule:${TAG},ghcr.io/${REPO_LOWER}-schedule:${VERSION}"
          if [ -n "$DOCKERHUB_TOKEN" ]; then
            TAGS="${TAGS},${DOCKERHUB_USER}/rylixmanager-schedule:${TAG}"
          fi
          echo "tags=${TAGS}" >> $GITHUB_OUTPUT

      - name: Build and push Schedule image
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./Dockerfile.schedule
          push: true
          tags: ${{ steps.meta-schedule.outputs.tags }}
          platforms: linux/amd64

  build-and-push-server-image:
    runs-on: ubuntu-latest
    env:
      DOCKERHUB_USER: ${{ secrets.DOCKERHUB_USERNAME || 'imrishmika' }}
      DOCKERHUB_TOKEN: ${{ secrets.DOCKERHUB_TOKEN }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Log in to Docker Hub
        if: env.DOCKERHUB_TOKEN != ''
        uses: docker/login-action@v3
        with:
          username: ${{ env.DOCKERHUB_USER }}
          password: ${{ env.DOCKERHUB_TOKEN }}

      - name: Set tag and version
        id: meta-server
        run: |
          VERSION=$(jq -r .version apps/dokploy/package.json)
          REPO_LOWER=$(echo "${{ github.repository }}" | tr '[:upper:]' '[:lower:]')
          TAG="latest"
          if [ "${{ github.ref }}" != "refs/heads/main" ]; then
            TAG="canary"
          fi
          TAGS="ghcr.io/${REPO_LOWER}-server:${TAG},ghcr.io/${REPO_LOWER}-server:${VERSION}"
          if [ -n "$DOCKERHUB_TOKEN" ]; then
            TAGS="${TAGS},${DOCKERHUB_USER}/rylixmanager-server:${TAG}"
          fi
          echo "tags=${TAGS}" >> $GITHUB_OUTPUT

      - name: Build and push Server image
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./Dockerfile.server
          push: true
          tags: ${{ steps.meta-server.outputs.tags }}
          platforms: linux/amd64
